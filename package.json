{
  "name": "muffinman-blog",
  "author": "Stanko",
  "scripts": {
    "build": "rm -rf ./public/assets && parcel build js/app.js --no-source-maps --out-dir public/assets --public-url /public/assets",
    "assets": "rm -rf ./public/assets && parcel watch js/app.js --no-source-maps --out-dir public/assets --public-url /public/assets",
    "jekyll": "jekyll serve --incremental --livereload --host 0.0.0.0",
    "start": "rm -rf _site && npm run build && npm run jekyll"
  },
  "license": "SEE LICENSE IN LICENSE.md",
  "devDependencies": {
    "autoprefixer": "^9.5.1",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "node-sass": "^4.11.0",
    "parcel-bundler": "^1.12.3"
  },
  "dependencies": {
    "animated-scroll-to": "^1.1.7",
    "window-scroll-manager": "^0.2.1"
  }
}
